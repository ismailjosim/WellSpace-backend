generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // connection URL moved to prisma.config.ts for Prisma 7+ (used for Migrate and client configuration)
  // See: https://pris.ly/d/config-datasource and https://pris.ly/d/prisma7-client-config
}

// we don't need to add any export of any file
